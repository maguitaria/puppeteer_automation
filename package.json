{
  "name": "automation",
  "version": "1.0.0",
  "description": "Project to download the menu from local student`s restaurant in a headless mode",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security",
    "puppeteer": "^21.3.8"
  }
}
